export function LoginFooter() {
  return (
    <footer className="text-center">
      <small className="text-muted-foreground/40 text-[11px] font-medium tracking-wider uppercase">
        Full Calzado &copy; {new Date().getFullYear()} — Cuantiva
      </small>
    </footer>
  );
}
